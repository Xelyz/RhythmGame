using UnityEngine;

public class SceneReadyByDefault : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Transfer.sceneReady = true;
    }
}
